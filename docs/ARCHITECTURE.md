# workflow-strt Architecture
Last updated: 2026-08-20
Source branch: `cursor/media-required-optional-fix-cd21`
Source commit: `5acfbba`

This file is generated by `scripts/update-architecture-docs.mjs`. Do not hand-edit it unless you are also updating the generator.

## Overview

workflow-strt is a shared web/mobile field operations app with:

| Target | Implementation | Entry |
|---|---|---|
| Web browser | React + Vite (commtrac-project-studio 0.1.0) | http://<host>:5173 |
| Mobile wrappers | Capacitor Android + iOS using the same React bundle | android/ and ios/ |
| Backend API | ASP.NET Core + SQLite | http://0.0.0.0:4000 |

## Runtime shape

- Frontend router: `BrowserRouter`
- Vite dev server: host `0.0.0.0`, port `5173`, strictPort `true`
- API default expectation: `http://<host>:4000/api` or `VITE_API_BASE`
- Shared shell: `AppShell` wraps authenticated routes

## Auth and app boot

- Biometric/PIN lock screen supported on native-capable flows
- Login screen shown when there is no active session or grace period expired
- BrowserRouter drives the web app shell and page routing

## Main shell

- Desktop layout uses a left sidebar.
- Mobile layout uses a bottom tab bar.
- Global banners and sync status render above page content.
- Main page content is wrapped in pull-to-refresh.

## Route map

| Path | Component | Auth | Notes |
|---|---|---|---|
| `/login` | `Login` | Public |  |
| `/reset-password` | `ResetPassword` | Public |  |
| `/sign/:tokenId` | `ExternalSignPage` | Public |  |
| `/share/reports/:shareId` | `AssetReportShareViewPage` | Required |  |
| `/` | `Dashboard` | Required |  |
| `/projects` | `ProjectsPage` | Required |  |
| `/projects/new` | `ProjectForm` | Required |  |
| `/projects/:id` | `ProjectDetail` | Required |  |
| `/projects/:id/edit` | `ProjectForm` | Required |  |
| `/projects/:id/inspections/inbox` | `ProjectDetail` | Required |  |
| `/projects/:id/inspections` | `ProjectInspectionsRedirect` | Required | Redirect route |
| `/projects/:id/assets/:assetId/inspections` | `ProjectAssetInspectionPage` | Required |  |
| `/installations/assets` | `AssetInstallationPage` | Required |  |
| `/installations/capture` | `CaptureTablePage` | Required |  |
| `/work-instructions` | `WorkInstructionsRoute` | Required |  |
| `/documents` | `DocumentsPage` | Required |  |
| `/tips` | `TipsRoute` | Required |  |
| `/admin` | `AdminRoute` | Required |  |
| `/admin/customers/:customerId/sites` | `CustomerSitesRoute` | Required |  |
| `/admin/fault-reports` | `FaultReportsRoute` | Required |  |
| `/issues` | `IssuesBoard` | Required |  |
| `/time-analytics` | `TimeAnalyticsRoute` | Required |  |
| `/time-analytics/:view` | `TimeAnalyticsRoute` | Required |  |
| `/settings` | `SettingsRoute` | Required | Permission-gated by SettingsRoute |
| `/profile` | `ProfileWizard` | Required |  |
| `/admin/bom-project` | `BomDashboard` | Required | Shown only when BOM module is enabled |
| `/admin/bom-project/upload` | `BomUploadPage` | Required | Shown only when BOM module is enabled |
| `/admin/bom-project/imports/:id/mapping` | `BomMappingPage` | Required | Shown only when BOM module is enabled |
| `/admin/bom-project/imports/:id/classification` | `BomClassificationPage` | Required | Shown only when BOM module is enabled |
| `/admin/bom-project/imports/:id/commit` | `BomCommitPage` | Required | Shown only when BOM module is enabled |
| `/mobile-upload` | `MobileUploadPage` | Required | Standalone upload flow |
| `*` | `Navigate` | Required | Redirect route |

## Desktop navigation

| Label | Path |
|---|---|
| `Dashboard` | `/` |
| `Projects` | `/projects` |
| `Issues Board` | `/issues` |
| `Assets` | `/installations/assets` |
| `Workflows` | `/work-instructions` |
| `Documents` | `/documents` |
| `Tips & Tricks` | `/tips` |
| `BOM to Project` | `/admin/bom-project` |
| `Time Analytics` | `/time-analytics` |
| `Admin` | `/admin` |
| `Fault Reports` | `/admin/fault-reports` |
| `Settings` | `/settings` |
| `Profile` | `/profile` |

## Mobile navigation

Primary tabs:

| Label | Path |
|---|---|
| `Home` | `/` |
| `Projects` | `/projects` |
| `Assets` | `/installations/assets` |
| `Issues` | `/issues` |
| `More` | `__more__` |

More drawer:

| Label | Path |
|---|---|
| `Workflows` | `/work-instructions` |
| `Documents` | `/documents` |
| `Tips & Tricks` | `/tips` |
| `Profile` | `/profile` |

## Backend surface

| Area | Controllers |
|---|---|
| Core | Auth, Dashboard, Health, Issues, ProjectAssets, Projects, Users |
| Workflow | AssetWorkflowAssignments, AssetWorkflowRuns, PublicSign, SignatureEvents, SignatureTokens, WorkflowConfigFeatures, WorkflowConfigs, WorkflowTemplates, WorkflowTypes, WorkInstructions, WorkInstructionTemplates |
| Admin/Config | Admin, AdminTabRows, AdminTabs, BrandSettings, CustomFields, FieldDefinitions, FieldValues, Offices, RoleConfigs, Settings, TableConfigs |
| Content/Sync | AssetDocumentLinks, AssetDocuments, Documents, InspectionImports, MobileUpload, Notifications, Search, Sse |

All controllers discovered from `server/Commtrac.Api/Controllers`: `AdminController`, `AdminTabRowsController`, `AdminTabsController`, `AssetDocumentLinksController`, `AssetDocumentsController`, `AssetReportSharesController`, `AssetWorkflowAssignmentsController`, `AssetWorkflowRunsController`, `AuthController`, `BomImportRunsController`, `BrandSettingsController`, `CustomersController`, `CustomFieldsController`, `DashboardController`, `DivisionsController`, `DocumentsController`, `FaultReportsController`, `FeatureDependenciesController`, `FeaturesController`, `FieldDefinitionsController`, `FieldValuesController`, `HealthController`, `InspectionImportsController`, `InstallationsController`, `IssuesController`, `MobileUploadController`, `NotificationsController`, `OfficesController`, `ProductsController`, `ProjectAssetsController`, `ProjectContactsController`, `ProjectInboundItemsController`, `ProjectsController`, `PublicSignController`, `PushTokensController`, `QuickbaseDiscoveryController`, `QuickbaseGoodsMovementsController`, `RoleConfigsController`, `SearchController`, `SettingsController`, `SignatureEventsController`, `SignatureTokensController`, `SitesController`, `SseController`, `SyncChangesController`, `TableConfigsController`, `TimeAnalyticsController`, `UsersController`, `WorkflowConfigFeaturesController`, `WorkflowConfigsController`, `WorkflowTemplatesController`, `WorkflowTypesController`, `WorkInstructionsController`, `WorkInstructionTemplatesController`

## Key directories

| Path | Purpose |
|---|---|
| `src/app` | App bootstrap and route table |
| `src/components/layout` | Shared shell, sidebar, topbar, mobile tabs |
| `src/features` | Page-level feature modules |
| `src/services` | API/domain service layer and local data helpers |
| `src/modules/bom-project` | Feature-flagged BOM-to-Project workflow |
| `server/Commtrac.Api` | ASP.NET Core API, controllers, services, EF/SQLite |
| `docs` | Generated architecture doc plus supporting reference docs |

## Docs automation

- Regenerate docs manually with `npm run docs:update`
- The repo pre-commit hook runs the same generator and stages `docs/ARCHITECTURE.md`
- Hook install command: `npm run hooks:install`
- This generator intentionally avoids spawning git so it can run under restrictive environments
- The docs snapshot reflects the checked-out files and current HEAD ref/commit

