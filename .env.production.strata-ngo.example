# Strata N-Go — PROD web/native build profile (build only until Phase C/F cutover)
# Copy to .env.production.strata-ngo.local (git-ignored) before npm run build:prod-web

VITE_API_BASE=https://api.strata-ngo.com/api
VITE_APP_ENV=prod
