{
  "name": "commtrac-sales-presentation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "pack": "npm run build && cd dist && zip -r ../commtrac-sales-presentation.zip . && cp ../commtrac-sales-presentation.zip ../dist-ready/",
    "preview": "vite preview",
    "narration": "node scripts/generate-narration.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
