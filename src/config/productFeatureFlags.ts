/**
 * Product feature gates that are not ready for field use.
 * Flip to true when the feature is complete enough to show in the UI.
 */
export const INSPECTION_ASSETS_UI_ENABLED = false;
export const INSPECTION_INBOX_UI_ENABLED = false;
