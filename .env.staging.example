# Staging web build — copy to .env.staging.local (git-ignored)
VITE_API_BASE=https://api.staging.yourdomain.com/api

# BOM module — enabled for staging demos (Admin / PM roles have access by default)
VITE_ENABLE_BOM_MODULE=true
