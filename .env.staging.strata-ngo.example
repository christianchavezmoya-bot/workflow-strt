# Strata N-Go — DEV web/native build profile
# Copy to .env.staging.strata-ngo.local (git-ignored) or rely on build:dev-web defaults.

VITE_API_BASE=https://api.staging.strata-ngo.com/api
VITE_APP_ENV=dev

VITE_ENABLE_BOM_MODULE=true
